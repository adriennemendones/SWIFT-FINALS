import SwiftUI

struct ExploreView: View {
    var body: some View {
        VStack {
            Text("Explore")
                .font(.largeTitle)
                .fontWeight(.bold)
            Spacer()
        }
    }
}

#Preview {
    ExploreView()
}

//
